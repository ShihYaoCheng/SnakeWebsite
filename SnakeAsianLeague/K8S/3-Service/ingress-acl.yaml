apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: asianleague-ingress
  annotations:
    # kubernetes.io/ingress.global-static-ip-name: beta-snake-acl-address  
    # networking.gke.io/managed-certificates: certificate-snakeacl
    kubernetes.io/ingress.class: "gce"
spec:
  defaultBackend:
    service:
      name: asianleague-web
      port:
        number: 80