@using Microsoft.AspNetCore.Components.Web


@code {
  @* [Parameter]
    public Boolean RankIsGuild { get; set; }

    
    [Parameter]
    public EventCallback<MouseEventArgs,Boolean> ToggleIsGuild { get; set;}

    private void ClickGuildBTN(MouseEventArgs e, bool isguild)
    {
        ToggleIsGuild.InvokeAsync(e, isguild); 
    }
   *@
}